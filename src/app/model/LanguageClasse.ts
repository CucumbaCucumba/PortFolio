import * as internal from "assert";
import { Competence } from "./Language";

export class CompetenceClasse implements Competence{
    constructor() {
        
    }
    id: number;
    nom: string;
}